# Real-Time-Packet-Analyzer
A real-time network packet analyzer built with Flask and Scapy, featuring live capture, filtering, protocol analysis, and session logging via an interactive web dashboard.
